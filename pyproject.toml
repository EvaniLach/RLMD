[tool.poetry]
name = "poetry"
version = "0.1.0"
description = "config file"
authors = ["'Evani' <'evanilach@gmail.com'>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
